# webpack-demo

1. npm i;
2. npm run build;
3. Now, in the dist/index.html, you can see that JavaScript in script elements is not compressed.
4. And, if you remove comments and safari10 in the webpack.config.js, then repeat the steps above, JavaScript in script elements is compressed.
